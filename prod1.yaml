apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: wather-app-1
  name: wather-app-1
  namespace: jenkins
spec:
  replicas: 3
  selector:
    matchLabels:
      app: wather-app-1
  template:
    metadata:
      labels:
        app: wather-app-1
    spec:
      containers:
        - image: registry.cosmin-lab.cloud:5000/wather-app:1
          imagePullPolicy: Always
          name: wather-app
      imagePullSecrets:
        - name: regcred2

---

apiVersion: v1
kind: Service
metadata:
  name: myweb-service  
  namespace: jenkins
spec:
  selector:
    app: wather-app-1
  ports:
    - protocol: TCP
      port: 10  
      nodePort: 30201
  type: NodePort
