apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: wather-app-3
  name: wather-app-3
  namespace: jenkins
spec:
  replicas: 1
  selector:
    matchLabels:
      app: wather-app-3
  template:
    metadata:
      labels:
        app: wather-app-3
    spec:
      containers:
        - image: registry.cosmin-lab.cloud:5000/wather-app:3
          imagePullPolicy: Always
          name: wather-app
      imagePullSecrets:
        - name: regcred2

---

apiVersion: v1
kind: Service
metadata:
  name: myweb-service  
  namespace: jenkins
spec:
  selector:
    app: wather-app-3
  ports:
    - protocol: TCP
      port: 10  
      nodePort: 30203
  type: NodePort
